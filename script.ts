// tuples
let address: [string, number, string] = ["5225 Pooks Hill Rd", 523, 'North'];

// enums
enum Color {
  Gray,   // 0
  Green,  // 1
  Blue,   // 2
  Red = 777
}

let myColor: Color = Color.Red;
console.log(myColor);
